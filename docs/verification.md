# Результаты проверки — 16.09.2026

- Сборка Maven `package`: успешно, Java 21 с целевой версией 17.
- `verify.ps1`: ALL PASSED: 73, настоящий PostgreSQL 18; временная схема удалена.
- Проверены транзакционный откат и одновременное бронирование из двух потоков.
- `run.cmd`: успешная компиляция, запуск меню, статистика и экспорт всех трёх сущностей в XLSX и CSV.
- `compile.ps1`: успешная компиляция и корректное завершение при конце входного потока; кириллица читается.
- XLSX: структура ZIP/XML корректна; 5 клиентов, 8 предметов, 10 заявок (без заголовков).
- ER-диаграмма: корректный XML/SVG, три сущности и две связи 1:N.
- Исходная база rental_system сохранена: 4 клиента, 5 предметов, 0 заявок.
- Демонстрационная база rental_system_demo_20260916: 5 клиентов, 8 предметов, 10 заявок, 5 статусов.

Приложение использует исходные версии библиотек проекта. Проверки экспорта открывают XLSX через Apache POI; ручное открытие в Microsoft Excel не выполнялось. Полный интеграционный набор запускается через verify.ps1, а не через mvn test.
